<?xml version="1.0"?>
<!-- This file is part of the book                 -->
<!--                                               -->
<!--   Abstract Algebra: Theory and Applications   -->
<!--                                               -->
<!-- Copyright (C) 1997-2014  Thomas W. Judson     -->
<!-- See the file COPYING for copying conditions.  -->
<!--                                               -->
<!-- This file is automatically generated from the -->
<!-- private solution manual.  Minor contributed   -->
<!-- edits can be made against this file for       -->
<!-- the purpose of a pull request.                -->
<exercises xml:id="vect-exercises">
	<title>Exercises</title>

	<exercise number="1">
		<statement>
			<p>If <m>F</m> is a field, show that <m>F[x]</m> is a vector space over <m>F</m>, where the vectors in <m>F[x]</m> are polynomials. Vector addition is polynomial addition, and scalar multiplication is defined by <m>\alpha p(x)</m> for <m>\alpha \in F</m>.</p>
		</statement>
		
	</exercise>

	<exercise number="2">
		<statement>
			<p>Prove that <m>{\mathbb Q }( \sqrt{2}\, )</m> is a vector space.</p>
		</statement>
		
	</exercise>

	<exercise number="3">
		<statement>
			<p>Let <m>{\mathbb Q }( \sqrt{2}, \sqrt{3}\, )</m> be the field generated by elements of the form <m>a + b \sqrt{2}  + c \sqrt{3}</m>, where <m>a, b, c</m> are in <m>{\mathbb Q}</m>. Prove that <m>{\mathbb Q }( \sqrt{2}, \sqrt{3}\, )</m> is a vector space of dimension 4 over <m>{\mathbb Q}</m>.  Find a basis for <m>{\mathbb Q }( \sqrt{2}, \sqrt{3}\, )</m>.</p>
		</statement>
		<hint>
			<p><m>{\mathbb Q}(\sqrt{2}, \sqrt{3}\, )</m> has basis <m>\{ 1, \sqrt{2}, \sqrt{3}, \sqrt{6}\, \}</m>  over <m>{\mathbb Q}</m>.</p>
		</hint>
		
	</exercise>

	<exercise number="4">
		<statement>
			<p>Prove that the complex numbers are a vector space of dimension 2 over <m>{\mathbb R}</m>.</p>
		</statement>
		
	</exercise>

	<exercise number="5">
		<statement>
			<p>Prove that the set <m>P_n</m> of all polynomials of degree less than <m>n</m> form a subspace of the vector space <m>F[x]</m>. Find a basis for <m>P_n</m> and compute the dimension of~<m>P_n</m>.</p>
		</statement>
		<hint>
			<p><m>P_n</m> has basis <m>\{ 1, x, x^2, \ldots, x^{n-1} \}</m>.</p>
		</hint>
		
	</exercise>

	<exercise number="6">
		<statement>
			<p>Let <m>F</m> be a field and denote the set of <m>n</m>-tuples of <m>F</m> by <m>F^n</m>. Given vectors <m>u = (u_1, \ldots, u_n)</m> and <m>v = (v_1, \ldots, v_n)</m> in <m>F^n</m> and <m>\alpha</m> in <m>F</m>, define vector addition by
				<me>u + v = (u_1, \ldots, u_n) + (v_1, \ldots, v_n) = (u_1 + v_1, \ldots, u_n + v_n)</me>
			and scalar multiplication by 
				<me>\alpha u = \alpha(u_1, \ldots, u_n)= (\alpha u_1, \ldots, \alpha u_n).</me>
			Prove that <m>F^n</m> is a vector space of dimension <m>n</m> under these operations.</p>
		</statement>
		
	</exercise>

	<exercise number="7">
		<statement>
			<p>Which of the following sets are subspaces of <m>{\mathbb R}^3</m>? If the set is indeed a subspace, find a basis for the subspace and compute its dimension.
				<ol>

					<li><m>\{ (x_1, x_2, x_3) : 3 x_1 - 2 x_2 + x_3 = 0 \}</m></li>

					<li><m>\{ (x_1, x_2, x_3) : 3 x_1 + 4 x_3 = 0, 2 x_1 - x_2 + x_3 = 0 \}</m></li>

					<li><m>\{ (x_1, x_2, x_3) : x_1 - 2 x_2 + 2 x_3 = 2 \}</m></li>

					<li><m>\{ (x_1, x_2, x_3) : 3 x_1 - 2 x_2^2 = 0 \}</m></li>

				</ol></p>
		</statement>
		<hint>
			<p>(a) Subspace of dimension 2 with basis <m>\{(1, 0, -3), (0, 1, 2) \}</m>; (d) not a subspace</p>
		</hint>
		
	</exercise>

	<exercise number="8">
		<statement>
			<p>Show that the set of all possible solutions <m>(x, y, z) \in {\mathbb R}^3</m> of the equations
				<md>
					<mrow>Ax + B y + C z &amp; =  0</mrow>
					<mrow>D x + E y + C z &amp; =  0</mrow>
				</md>
			form a subspace of <m>{\mathbb R}^3</m>.</p>
		</statement>
		
	</exercise>

	<exercise number="9">
		<statement>
			<p>Let <m>W</m> be the subset of continuous functions on <m>[0, 1]</m> such that <m>f(0) = 0</m>.  Prove that <m>W</m> is a subspace of <m>C[0, 1]</m>.</p>
		</statement>
		
	</exercise>

	<exercise number="10">
		<statement>
			<p>Let <m>V</m> be a vector space over <m>F</m>. Prove that <m>-(\alpha v) = (-\alpha)v = \alpha(-v)</m> for all <m>\alpha \in F</m> and all <m>v \in V</m>.</p>
		</statement>
		<hint>
			<p><m>0 =  \alpha 0 = \alpha(-v + v) = \alpha(-v) + \alpha v \Rightarrow - \alpha v = \alpha(-v)</m>.</p>
		</hint>
		
	</exercise>

	<exercise number="11">
		<statement>
			<p>Let <m>V</m> be a vector space of dimension <m>n</m>. Prove each of the following statements. 
				<ol>

					<li>If <m>S = \{v_1, \ldots, v_n \}</m> is a set of linearly independent vectors for <m>V</m>, then <m>S</m> is a basis for <m>V</m>.</li>

					<li>If <m>S = \{v_1, \ldots, v_n \}</m> spans <m>V</m>, then <m>S</m> is a basis for <m>V</m>.</li>

					<li>If <m>S = \{v_1, \ldots, v_k \}</m> is a set of linearly independent vectors for <m>V</m> with <m>k \lt n</m>, then there exist vectors <m>v_{k + 1}, \ldots, v_n</m> such that 
						<me>\{v_1, \ldots, v_k, v_{k + 1}, \ldots, v_n \}</me> 
					is a basis for <m>V</m>.</li>

				</ol></p>
		</statement>
		
	</exercise>

	<exercise number="12">
		<statement>
			<p>Prove that any set of vectors containing <m>{\mathbf 0}</m> is linearly dependent.</p>
		</statement>
		<hint>
			<p>Let <m>v_0 = 0, v_1, \ldots, v_n \in V</m> and <m>\alpha_0 \neq 0, \alpha_1, \ldots, \alpha_n \in F</m>. Then <m>\alpha_0 v_0 + \cdots + \alpha_n v_n = 0</m>.</p>
		</hint>
		
	</exercise>

	<exercise number="13">
		<statement>
			<p>Let <m>V</m> be a vector space. Show that <m>\{ {\mathbf 0} \}</m> is a subspace of <m>V</m> of dimension zero.</p>
		</statement>
		
	</exercise>

	<exercise number="14">
		<statement>
			<p>If a vector space <m>V</m> is spanned by <m>n</m> vectors, show that any set of <m>m</m> vectors in <m>V</m> must be linearly dependent for <m>m \gt n</m>.</p>
		</statement>
		
	</exercise>

	<exercise number="15" xml:id="exercise-vect-linear-transformation">
		<title>Linear Transformations</title>
		<statement>
			<p>Let <m>V</m> and <m>W</m> be vector spaces over a field <m>F</m>, of dimensions <m>m</m> and <m>n</m>, respectively. If <m>T: V \rightarrow W</m> is a map satisfying
				<md>
					<mrow>T( u+ v ) &amp; =  T(u ) + T(v)</mrow>
					<mrow>T( \alpha v ) &amp; =  \alpha T(v)</mrow>
				</md>
			for all <m>\alpha \in F</m> and all <m>u, v \in V</m>, then <m>T</m> is called a <term>linear transformation</term> from <m>V</m> into <m>W</m>. 
				<ol>

					<li>Prove that the <term>kernel</term> of <m>T</m>, <m>\ker(T) = \{ v \in V : T(v) = {\mathbf 0} \}</m>, is a subspace of <m>V</m>. The kernel of <m>T</m> is sometimes called the <term>null space</term> of <m>T</m>.</li>

					<li>Prove that the <term>range</term> or <term>range space</term> of <m>T</m>, <m>R(V) = \{ w \in W : T(v) = w \text{ for some } v \in V \}</m>, is a subspace of <m>W</m>.</li>

					<li>Show that <m>T : V \rightarrow W</m> is injective if and only if <m>\ker(T) = \{ \mathbf 0 \}</m>.</li>

					<li>Let <m>\{ v_1, \ldots, v_k \}</m> be a basis for the null space of <m>T</m>. We can extend this basis to be a basis <m>\{ v_1, \ldots, v_k, v_{k + 1}, \ldots, v_m\}</m> of <m>V</m>. Why?  Prove that <m>\{ T(v_{k + 1}), \ldots, T(v_m) \}</m> is a basis for the range of <m>T</m>. Conclude that the range of <m>T</m> has dimension <m>m-k</m>.</li>

					<li>Let <m>\dim V = \dim W</m>.  Show that a linear transformation <m>T : V \rightarrow W</m> is injective if and only if it is surjective.</li>

				</ol></p>
		</statement>
		<hint>
			<p>(a) Let <m>u, v \in \ker(T)</m> and <m>\alpha \in F</m>.  Then
				<md>
					<mrow>T(u +v) = T(u) + T(v) = 0</mrow>
					<mrow>T(\alpha v) = \alpha T(v) = \alpha 0 = 0.</mrow>
				</md>
			Hence, <m>u + v, \alpha v \in \ker(T) \Rightarrow \ker(T)</m> is a subspace of <m>V</m>.</p>

			<p>(c) <m>T(u) = T(v) \Leftrightarrow T(u-v) = T(u) - T(v) = 0 \Leftrightarrow u-v = 0 \Leftrightarrow u = v</m>.</p>
		</hint>
		
	</exercise>

	<exercise number="16">
		<statement>
			<p>Let <m>V</m> and <m>W</m> be finite dimensional vector spaces of dimension <m>n</m> over a field <m>F</m>. Suppose that  <m>T: V \rightarrow W</m> is a vector space isomorphism.  If <m>\{ v_1, \ldots, v_n \}</m> is a basis of <m>V</m>, show that <m>\{ T(v_1), \ldots, T(v_n) \}</m> is a basis of <m>W</m>. Conclude that any vector space over a field <m>F</m> of dimension <m>n</m> is isomorphic to <m>F^n</m>.</p>
		</statement>
		
	</exercise>

<exercise number="17">
	<statement>
		<p><b>Direct Sums.</b> Let <m>U</m> and <m>V</m> be subspaces of a vector space <m>W</m>. The sum of <m>U</m> and <m>V</m>, denoted <m>U + V</m>, is defined to be the set of all vectors of the form <m>u + v</m>, where <m>u \in U</m> and <m>v \in V</m>. 
			<ol>

				<li>Prove that <m>U + V</m> and <m>U \cap V</m> are subspaces of <m>W</m>.</li>

				<li>If <m>U + V = W</m> and <m>U \cap V = {\mathbf 0}</m>, then <m>W</m> is said to be the <term>direct sum.</term>  Show that every element <m>w \in W</m> can be written uniquely as <m>w = u + v</m>, where <m>u \in U</m> and <m>v \in V</m>.</li>

				<li>Let <m>U</m> be a subspace of dimension <m>k</m> of a vector space <m>W</m> of dimension <m>n</m>. Prove that there exists a subspace <m>V</m> of dimension <m>n-k</m> such that <m>W = U \oplus V</m>.  Is the subspace <m>V</m> unique?</li>

				<li>If <m>U</m> and <m>V</m> are arbitrary subspaces of a vector space <m>W</m>, show that 
				<me>\dim( U + V) = \dim U + \dim V - \dim( U \cap V).</me></li>

			</ol></p>
	</statement>
	<hint>
		<p>(a) Let <m>u, u' \in U</m> and <m>v, v' \in V</m>. Then
			<md>
				<mrow>(u + v) + (u' + v') &amp; = (u + u') + (v + v') \in U + V</mrow>
				<mrow>\alpha(u + v) &amp; = \alpha u + \alpha v \in U + V.</mrow>
			</md></p>
		</hint>
	
</exercise>

	<exercise number="18">
		<title>Dual Spaces</title>
		<statement>
			<p>Let <m>V</m> and <m>W</m> be finite dimensional vector spaces over a field~<m>F</m>. 
				<ol>

					<li>Show that the set of all linear transformations from <m>V</m> into <m>W</m>,denoted by <m>\Hom(V, W)</m>,is a vector space over <m>F</m>, where wedefine vector addition as follows:
						<md>
							<mrow>(S + T)(v) &amp; =  S(v) +T(v)</mrow>
							<mrow>(\alpha S)(v) &amp; =  \alpha S(v),</mrow>
						</md>
					where <m>S, T \in \Hom(V, W)</m>, <m>\alpha \in F</m>, and <m>v \in V</m>.</li>
			 
					<li>Let <m>V</m> be an <m>F</m>-vector space. Define the <term>dual space</term> of <m>V</m> to be <m>V^\ast = \Hom(V, F)</m>\label{notedual}. Elements in the dual space of <m>V</m> are called <term>linear functionals.</term>  Let <m>v_1, \ldots, v_n</m> be an ordered basis for <m>V</m>. If <m>v = \alpha_1 v_1 + \cdots + \alpha_n v_n</m> is any vector in <m>V</m>, define a linear functional  <m>\phi_i : V \rightarrow F</m> by <m>\phi_i (v) = \alpha_i</m>. Show that the <m>\phi_i</m>'s form a basis for <m>V^\ast</m>.  This basis is called the <term>dual basis</term> of <m>v_1, \ldots, v_n</m> (or simply the dual basis if the context makes the meaning clear).</li>
			 
					<li>Consider the basis <m>\{ (3, 1), (2, -2) \}</m> for <m>{\mathbb R}^2</m>. What is the dual basis for <m>({\mathbb R}^2)^\ast</m>?</li>

					<li>Let <m>V</m> be a vector space of dimension <m>n</m> over a field <m>F</m> and let <m>V^{\ast \ast}</m> be the dual space <m>V^\ast</m>.  Show that each element <m>v \in V</m> gives rise to an element <m>\lambda_v</m> in <m>V^{\ast \ast}</m> and that the map <m>v \mapsto \lambda_v</m> is an isomorphism of <m>V</m> with <m>V^{\ast \ast}</m>.</li>

			</ol></p>
		</statement>
		
	</exercise>

</exercises>
